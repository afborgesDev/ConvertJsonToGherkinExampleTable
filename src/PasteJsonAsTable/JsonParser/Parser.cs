namespace PasteJsonAsTable.JsonParser
{
    public static class Parser
    {
        public static string Parse(string payload)
        {
            if (string.IsNullOrEmpty(payload))
                return string.Empty;

            return string.Empty;
        }
    }
}